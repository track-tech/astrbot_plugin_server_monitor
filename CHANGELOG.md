# 更新日志

本文件记录 astrbot_plugin_server_monitor 的版本变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本规范。

## [1.11.0] - 2026-09-03

### 新增

- **时间轴范围自定义**：「服务器卡片」与「历史图表」面板的设置编辑器新增「范围」选项（全部历史 / 10 分钟 / 30 分钟 / 1 小时 / 3 / 6 / 12 / 24 小时），按面板独立生效并随布局持久化。
- 插件配置「历史数据点数」默认值由 360 提升至 720（本机 5s 间隔下约 1 小时历史），并说明其与面板「范围」的关系。

## [1.10.1] - 2026-09-01

### 修复

- **看板布局服务端持久化完全失效**：`api_ui_save` / `api_ui_load` 将模块级函数 `_ui_state_path()` 误写为 `self._ui_state_path()`，导致保存请求在真实 AstrBot 环境中始终抛出 `AttributeError`，而读取侧的异常被静默吞掉、永远返回空状态——拖拽布局、刷新间隔、自定义布局预设因此全部无法持久化。
- 持久化路径解析增加插件安装目录兜底；路径完全不可用时返回明确错误并记录日志，不再静默丢弃。
- 请求体解析改用 `request.json(default={})` 官方写法，兼容 AstrBot Web 请求代理签名。

### 改进

- 页面保存失败时以 toast 提示（同内容 5 秒去重），不再无声失败。
- 启动日志输出看板状态文件的实际位置，便于排查。
- `overview` 接口返回 `plugin_version` 字段，页面页脚版本号随之动态更新，不再硬编码。

### 验证

- 已在真实 AstrBot 4.27.5 + 官方 Dashboard 中端到端验证：拖拽顺序、刷新间隔、布局预设保存后重载均正确恢复。

## [1.10.0] - 2026-08-31

### 新增

- 终端面板视觉重设计（Gnome Terminal 风格）：扁平标签栏、主题化配色、深浅主题适配。
- 终端支持同一目标多个并发会话；「新建会话」按钮强制新开标签，不再复用已有会话。

### 修复

- 终端面板「新建会话」按钮在目标标签已存在时无响应的问题。

### 改进

- 拖拽排序后布局即标记为自定义（不再被自动布局覆盖）。
- 刷新间隔修改同步持久化到服务端；自定义布局预设支持服务端保存（`ui/save` 字段级合并，`items` 与 `saved` 互不覆盖）。

## 历史版本

- **v1.7 / v1.7.3**：看板「终端」面板升级为持久交互式网页 SSH 会话（xterm.js + SSE）；Docker 容器三端采集增强。
- **v1.4**：模型调用统计（挂钩 LLM 请求/响应）、模型余额查询（DeepSeek / Moonshot / SiliconFlow / OpenAI Billing / One-API·New-API 中转）。
- **v1.3**：模块化面板看板（8 种面板自由拼接）、TCP 端口与 HTTP 服务探活、Docker 容器 / Top 进程面板。
